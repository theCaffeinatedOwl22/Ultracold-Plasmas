#ifndef TEST2_h
#define TEST2_h

#include <vector>

struct Units{ // testing explanations
    double r; // space unit for simulation in SI
    double t; // time unit for simulation in SI
    double E; // energy unit for simulation in SI
};

enum Axes{ax_x,ax_y,ax_z,ax_num}; // cartesian axes for simulation

#endif